# Acceso coordinador

La versión pública usa una tabla compartida en Supabase para nuevas tipificaciones. El botón para crear tipificaciones solo se habilita tras validar las credenciales del coordinador contra una función de servidor.
